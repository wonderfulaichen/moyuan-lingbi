# 墨渊灵笔 全面重构计划 v4

> **原则**：UI 美术设计和布局不变。只重构架构、状态管理、服务层。
> **参考**：Roo-Code（状态推导、双 Store、Hook 拆分、流式防抖）、chinese-novelist-skill-master（Plan 持久化、五阶段流水线）。

---

## 一、当前问题全景

### A. 死代码（~1448行）

| 文件 | 行数 | 状态 |
|------|------|------|
| `RightTaskBar.tsx` | ~722 | ✅ 已删除 |
| `aiActions.ts` | ~419 | ✅ 已删除 |
| `systemPromptBuilder.ts` | ~292 | ✅ 已删除 |
| `StepContent.tsx` | ~15 | ✅ 已删除 |

### B. AIAssistantService.ts（~1600行上帝类）

| 问题 | 详情 | 状态 |
|------|------|------|
| 单文件承担全部职责 | 消息管理 + Agent循环 + Tool解析 + Tool执行 + 文件确认 + Plan执行 + 对话持久化 + Agent管理 | ✅ 已拆分为 8 个模块 |
| 手动状态设置 | `updateAgentState()` 散布各处 | ✅ 改为纯函数推导 |
| 流式无防抖 | 每个 streaming chunk 直接 `this.emit()` | ✅ 150ms 防抖 |
| 自纠错逻辑脆弱 | `selfCorrectionAttempts < 2` 兜底有 bug | ✅ processWithAI 统一处理 |
| Tool 解析单策略 | 只有一种正则 | ✅ ToolParser 多策略解析 |

### C. AIAssistantPanel.tsx（~875行上帝组件）

| 问题 | 详情 | 状态 |
|------|------|------|
| 20+ useState | 各种状态散布 | ✅ 拆分为 5 个 hooks + 5 个子组件 |
| 三层状态手动同步 | prevProcessingRef 桥接 | ✅ useAIAssistantState 统一管理 |
| 渲染+逻辑混合 | 全在一个 JSX 里 | ✅ 组件化拆分 |

### D. AIStatusContext.tsx（280行）

| 问题 | 详情 | 状态 |
|------|------|------|
| 全量订阅 | 10+ 组件订阅整个 AIStatusInfo | ✅ 降级为 Zustand store 薄包装层 |
| 职责混杂 | 多种状态混在一个对象 | ✅ 状态核心迁移到 aiStatusStore |
| 与 Service 状态重叠 | isGenerating ≈ isProcessing | ✅ store 统一管理 |

### E. Feature 组件各自调 aiService（10+处）

| 问题 | 详情 | 状态 |
|------|------|------|
| 无统一模式 | 每个组件自己管 loading/error/tokenUsage | ✅ useAIGeneration hook 已创建，渐进迁移 |

---

## 二、目标架构

### 参考来源

| 参考 | 借鉴点 |
|------|--------|
| **Roo-Code** `agent-state.ts` | `detectAgentState(messages)` 纯函数推导状态，状态不可能不一致 |
| **Roo-Code** `store.ts` + `uiStateStore.ts` | Zustand 双 Store（任务态 + UI态），`resetForTaskSwitch()` 保留全局态 |
| **Roo-Code** 12 个 hooks | `useTaskSubmit` / `useMessageHandlers` / `useFocusManagement` 各司其职 |
| **Roo-Code** `STREAMING_DEBOUNCE_MS` | 流式 150ms 批量合并更新 |
| **chinese-novelist** `02-写作计划.json` | Plan 状态持久化到文件，支持中断续写 |
| **chinese-novelist** 五阶段流水线 | 初始化 → 问答 → 规划 → 创作 → 校验，每阶段有明确输入输出 |

### 架构图

```
┌──────────────────────────────────────────────────────────────┐
│                         App.tsx                               │
│  ┌──────────┐  ┌──────────────┐  ┌────────────────────────┐  │
│  │StepSidebar│  │ FileExplorer │  │  AIAssistantPanel      │  │
│  │(不变)     │  │(不变)        │  │  (拆分后 ~450行编排层)  │  │
│  └──────────┘  └──────────────┘  └───────────┬────────────┘  │
└──────────────────────────────────────────────┼───────────────┘
                                               │
                    ┌──────────────────────────┼──────────────────────┐
                    │                          │                      │
            ┌───────▼──────┐         ┌────────▼────────┐   ┌────────▼────────┐
            │useAIAssistant│         │ useChatInput    │   │ usePanelResize  │
            │State         │         │ (消息+输入)      │   │ (面板拖拽/折叠)  │
            └───────┬──────┘         └────────┬────────┘   └────────┬────────┘
                    │                         │                      │
                    └────────────┬────────────┘──────────────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │     aiStore (Zustand)    │
                    │  ┌─────────────────────┐│
                    │  │ messages[]           ││
                    │  │ agentState ← 推导    ││
                    │  │ streamingContent(防抖)││
                    │  │ pendingConfirm       ││
                    │  │ planState            ││
                    │  │ conversations[]      ││
                    │  │ agents[]             ││
                    │  └─────────────────────┘│
                    └────────────┬────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │     uiStore (Zustand)    │
                    │  inputText / activeTab   │
                    │  panelWidth / collapsed  │
                    │  editingMsgId / copyToast│
                    └─────────────────────────┘

                    ┌─────────────────────────┐
                    │  AIAssistantEngine       │  ← 纯逻辑，无状态
                    │  ├─ detectAgentState()   │  ← 纯函数推导
                    │  ├─ processWithAI()      │  ← 拆分为阶段函数
                    │  ├─ ToolParser           │  ← 多策略解析
                    │  ├─ UnifiedExecutor      │  ← 解析+验证+重试
                    │  └─ PlanExecutor         │  ← 持久化+续写
                    └─────────────────────────┘

                    ┌─────────────────────────┐
                    │  aiStatusStore (Zustand) │  ← AIStatusContext 降级
                    │  isGenerating / progress │
                    │  tokenUsage / tasks[]    │
                    │  AIStatusContext → 薄包装 │
                    └─────────────────────────┘

                    ┌─────────────────────────┐
                    │  useAIGeneration hook    │  ← Feature 组件统一入口
                    │  generate() / abort()    │
                    │  isGenerating / status   │
                    └─────────────────────────┘
```

---

## 三、分阶段执行计划

### Phase 0：清理死代码（零风险） ✅ 已完成

| # | 操作 | 文件 | 状态 |
|---|------|------|------|
| 0.1 | 删除 | `RightTaskBar.tsx` | ✅ |
| 0.2 | 删除 | `aiActions.ts` | ✅ |
| 0.3 | 删除 | `systemPromptBuilder.ts` | ✅ |
| 0.4 | 删除 | `StepContent.tsx` | ✅ |

**结果**：删除 ~1448 行无用代码，`npm run build` 通过。

---

### Phase 1：引入 Zustand + 状态重构 ✅ 已完成

#### 1.1 安装 Zustand ✅

#### 1.2 创建 `aiStore.ts`（任务/消息状态） ✅

文件：`src/renderer/shared/stores/aiStore.ts`（219行）

核心设计：
- `agentState` 通过 `detectAgentState()` 纯函数推导，不再手动设置
- `setStreamingContent()` 带 150ms 防抖
- `syncFromService()` 用于过渡期从旧服务同步状态
- `resetForNewTask()` 清任务态，保留全局态

#### 1.3 创建 `uiStore.ts`（UI 展示状态） ✅

文件：`src/renderer/shared/stores/uiStore.ts`（87行）

包含：inputText / activeTab / panelWidth / isExpanded / editingMsgId / collapsedIds / copyToast / showHistory / showAgentMenu / attachedFiles 等

#### 1.4 创建 `detectAgentState.ts` 纯函数 ✅

文件：`src/renderer/shared/services/ai-assistant/detectAgentState.ts`

---

### Phase 2：AIAssistantService 拆分 ✅ 已完成

#### 拆分后的文件结构

```
src/renderer/shared/services/ai-assistant/
├── index.ts                    ← AIAssistantService 瘦身后，re-export
├── detectAgentState.ts         ← 纯函数推导 (~80行)
├── processWithAI.ts            ← Agent 循环主体 (~200行)
├── ToolParser.ts               ← 多策略解析 (~150行)
├── UnifiedExecutor.ts          ← 解析+验证+重试 (~120行)
├── PlanExecutor.ts             ← Plan 持久化+续写 (~100行)
├── contextBuilder.ts           ← 项目上下文构建 (~200行)
├── systemPrompt.ts             ← SYSTEM_PROMPT + Agent prompts (~100行)
└── commandHandler.ts           ← 快捷命令 /角色 /地点 等 (~60行)
```

**结果**：`npm run build` 通过。

---

### Phase 3：AIAssistantPanel 拆分 ✅ 已完成

#### 3.1 拆分为子组件

```
src/renderer/app/app-shell/ai-assistant/components/
├── ChatHistory.tsx             ← 对话历史面板
├── MessageBubble.tsx           ← 单条消息气泡（用户/AI/流式）
├── PendingPromptRenderer.tsx   ← 输入/选择/Plan 提示渲染
├── AgentMenu.tsx               ← Agent 切换/编辑/创建
├── ConfirmPanel.tsx            ← 文件确认弹窗
└── index.ts                    ← re-export
```

#### 3.2 拆分为 Hooks

```
src/renderer/app/app-shell/ai-assistant/hooks/
├── useAIAssistantState.ts      ← AI 状态订阅和管理
├── useChatInput.ts             ← 输入框处理和文件附件
├── useChatScroll.ts            ← 滚动位置管理
├── useMessageActions.ts        ← 消息编辑/复制/撤回/重生成
├── usePanelResize.ts           ← 面板拖拽/折叠/宽度持久化
└── index.ts                    ← re-export
```

#### 3.3 流式更新防抖 ✅

在 `aiStore.ts` 中实现，150ms 批量合并更新。

**结果**：AIAssistantPanel 从 ~1274 行降到 ~450 行编排层，`npm run build` 通过。

---

### Phase 4：Feature 组件统一 AI 调用 ✅ Hook 已创建（渐进迁移）

#### 4.1 创建 `useAIGeneration` hook ✅

文件：`src/renderer/shared/hooks/useAIGeneration.ts`

```typescript
function useAIGeneration() {
  return {
    generate,       // 统一的 AI 生成调用
    abort,          // 中断生成
    isGenerating,   // 生成状态
    lastResult,     // 上次结果
    status,         // AIStatusInfo
    // AIStatusContext 方法透传
    setGenerating, setProgress, setStatusMessage, setTokenUsage,
    setComplete, setError, resetStatus,
  };
}
```

#### 4.2 Feature 组件迁移计划

| 组件 | 当前 | 替换为 | 状态 |
|------|------|--------|------|
| StepInspiration | `useAIStatus` + `aiService.generateWithContext` | `useAIGeneration` | 待迁移 |
| StepPlot | 同上 | `useAIGeneration` | 待迁移 |
| StepWorld | 同上 | `useAIGeneration` | 待迁移 |
| BubbleFolderContent | 同上 | `useAIGeneration` | 待迁移 |
| WritingEditor | 同上 | `useAIGeneration` | 待迁移 |
| DetailedOutlineView | 同上 | `useAIGeneration` | 待迁移 |
| InspirationIncubation | 同上 | `useAIGeneration` | 待迁移 |
| InspirationSchemes | 同上 | `useAIGeneration` | 待迁移 |
| OutlineView | 同上 | `useAIGeneration` | 待迁移 |
| StepReview | 同上 | `useAIGeneration` | 待迁移 |

**说明**：hook 已就绪，Feature 组件可在后续迭代中渐进迁移，不影响当前功能。

---

### Phase 5：AIStatusContext 降级 ✅ 已完成

#### 5.1 创建 `aiStatusStore` (Zustand) ✅

文件：`src/renderer/shared/stores/aiStatusStore.ts`（180行）

将 AIStatusContext 的全部状态逻辑迁移到 Zustand store：
- `setGenerating` / `setProgress` / `setStatusMessage` / `setTokenUsage`
- `setError` / `setComplete` / `resetStatus`
- `addTask` / `setActiveTask` / `clearCompletedTasks`
- 多任务管理（tasks[] 数组）

#### 5.2 AIStatusContext 改为薄包装层 ✅

文件：`src/renderer/shared/contexts/AIStatusContext.tsx`（77行）

- 从 `useAIStatusStore()` 读取状态
- 保持所有消费者接口不变（`useAIStatus()` 返回类型一致）
- 消费者无需任何修改

**结果**：`npm run build` 通过。

---

## 四、执行顺序与依赖关系

```
Phase 0 (死代码清理) ✅
  │
  ▼
Phase 1 (Zustand + 状态重构) ✅
  │
  ├──────────────────┐
  ▼                  ▼
Phase 2 (Service拆分) ✅  Phase 3 (Panel拆分) ✅
  │                  │
  └────────┬─────────┘
           ▼
Phase 4 (Feature统一hook) ✅ Hook已创建，渐进迁移
           │
           ▼
Phase 5 (AIStatusContext降级) ✅
```

---

## 五、每个 Phase 的验收标准

| Phase | 验收标准 | 状态 |
|-------|---------|------|
| **0** | `npm run build` 通过；RightTaskBar/aiActions/systemPromptBuilder/StepContent 已删除 | ✅ |
| **1** | aiStore + uiStore 创建完成；`detectAgentState()` 纯函数工作 | ✅ |
| **2** | AIAssistantService 拆分为 8 个文件；ToolParser 多策略解析工作 | ✅ |
| **3** | AIAssistantPanel 拆分为 5 hooks + 5 子组件；流式防抖生效 | ✅ |
| **4** | `useAIGeneration` hook 已创建；Feature 组件可渐进迁移 | ✅ |
| **5** | AIStatusContext 降级为 Zustand 薄包装层；`npm run build` 通过 | ✅ |

---

## 六、重构成果总结

### 新增文件清单

| 文件 | 行数 | 职责 |
|------|------|------|
| `shared/stores/aiStore.ts` | 219 | AI 任务/消息状态（Zustand） |
| `shared/stores/uiStore.ts` | 87 | UI 展示状态（Zustand） |
| `shared/stores/aiStatusStore.ts` | 180 | AI 状态管理（Zustand，替代 AIStatusContext 核心） |
| `shared/hooks/useAIGeneration.ts` | 85 | Feature 组件统一 AI 生成 hook |
| `shared/services/ai-assistant/detectAgentState.ts` | ~80 | 纯函数状态推导 |
| `shared/services/ai-assistant/ToolParser.ts` | ~150 | 多策略工具解析 |
| `shared/services/ai-assistant/UnifiedExecutor.ts` | ~120 | 文件操作执行+验证+重试 |
| `shared/services/ai-assistant/PlanExecutor.ts` | ~100 | Plan 持久化+续写 |
| `shared/services/ai-assistant/contextBuilder.ts` | ~200 | 项目上下文构建 |
| `shared/services/ai-assistant/systemPrompt.ts` | ~100 | 系统提示词+Agent 定义 |
| `shared/services/ai-assistant/commandHandler.ts` | ~60 | 快捷命令处理 |
| `shared/services/ai-assistant/processWithAI.ts` | ~200 | Agent 循环主体 |
| `app/app-shell/ai-assistant/hooks/useAIAssistantState.ts` | ~50 | AI 状态订阅 hook |
| `app/app-shell/ai-assistant/hooks/useChatInput.ts` | ~80 | 输入框 hook |
| `app/app-shell/ai-assistant/hooks/useChatScroll.ts` | ~40 | 滚动管理 hook |
| `app/app-shell/ai-assistant/hooks/useMessageActions.ts` | ~80 | 消息操作 hook |
| `app/app-shell/ai-assistant/hooks/usePanelResize.ts` | ~40 | 面板缩放 hook |
| `app/app-shell/ai-assistant/components/ChatHistory.tsx` | ~80 | 对话历史组件 |
| `app/app-shell/ai-assistant/components/MessageBubble.tsx` | ~150 | 消息气泡组件 |
| `app/app-shell/ai-assistant/components/PendingPromptRenderer.tsx` | ~100 | 提示渲染组件 |
| `app/app-shell/ai-assistant/components/AgentMenu.tsx` | ~80 | Agent 菜单组件 |
| `app/app-shell/ai-assistant/components/ConfirmPanel.tsx` | ~60 | 确认面板组件 |

### 删除文件清单

| 文件 | 行数 | 原因 |
|------|------|------|
| `shared/components/RightTaskBar.tsx` | ~722 | 死代码 |
| `shared/components/aiActions.ts` | ~419 | 死代码 |
| `shared/components/systemPromptBuilder.ts` | ~292 | 死代码 |
| `app/app-shell/StepContent.tsx` | ~15 | 空壳组件 |

### 修改文件清单

| 文件 | 变化 | 说明 |
|------|------|------|
| `shared/contexts/AIStatusContext.tsx` | 280→77行 | 降级为 Zustand 薄包装层 |
| `shared/services/AIAssistantService.ts` | ~1600→re-export | 拆分为 8 个模块 |
| `app/app-shell/AIAssistantPanel.tsx` | ~1274→~450行 | 使用 hooks + 子组件 |

### 核心改进

1. **状态一致性**：`detectAgentState()` 纯函数推导，状态不可能不一致
2. **流式性能**：150ms 防抖减少不必要的重渲染
3. **工具解析可靠性**：多策略解析 + 智能兜底提取
4. **代码可维护性**：上帝类拆分为职责单一的模块
5. **渐进迁移**：所有消费者接口保持兼容，可逐步迁移到新架构

---

## 七、风险控制

| 风险 | 缓解措施 | 状态 |
|------|---------|------|
| Zustand 与现有 Context 冲突 | AIStatusContext 保留为兼容层 | ✅ 已缓解 |
| 拆分 Service 引入新 bug | 每拆一个文件立即 `npm run build` | ✅ 已验证 |
| 流式防抖导致文字延迟 | 150ms 是 Roo-Code 验证过的值 | ✅ 已实现 |
| Feature 组件迁移遗漏 | useAIGeneration hook 已就绪，渐进迁移 | ✅ 可控 |
| 路径引用错误 | 构建验证通过 | ✅ 已修复 |
