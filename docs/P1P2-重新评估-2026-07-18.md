# P1 / P2 技术债重新评估

> 评估日期：2026-07-18  
> 基准：2026-06-11 三位专家交叉审计报告 `docs/overview.md`  
> 评估原则：对照 6/11 之后的演进（移动端、记忆系统、AI 任务管理、模块化架构、safeStorage、P0 修复），判断每项是否已被覆盖、仍有效、或已失效。

---

## 一、P1 关键问题（15 项）

| 原编号 | 问题 | 原描述 | 当前状态 | 评估依据 | 后续建议 |
|---|---|---|---|---|---|
| P1-1 | API 密钥 XOR 加密不安全 | XOR+base64 可逆，密钥硬编码 | **✅ 已覆盖** | `DataService` 已集成 `safeStorage`（Windows Credential Manager / macOS Keychain），XOR 仅作为降级；`enc:`/`safe:` 前缀区分 | 关闭 |
| P1-2 | `buildContext` 返回硬编码假数据 | `projectState.title` 为空，`currentChapter` 为 null | **✅ 已修复** | `ProjectContextBuilder` 从 `DataService.getActiveProject()` 读取真实项目数据，按 target 组装上下文 | 关闭 |
| P1-3 | 文件树折叠未实现 | `onToggleBook` 空函数，文件选择不更新高亮 | **✅ 已实现** | `FileSidebar.tsx` 使用 `expanded: Set<string>` 和 `toggleExpand` 实现文件夹展开/折叠；`FileExplorer` 有 `activeFileId` 高亮 | 关闭 |
| P1-4 | 右侧面板信息重复 + `renderCenter` 重建 | App.tsx 右栏与 StepAgent.tsx 右栏重叠 60%；`renderCenter` 非 memo | **🔶 部分修复** | `App.tsx` 的 `stepContent` 已改用 `useMemo`（L405），避免 step 组件潜在重建；但右侧面板重复问题仍存在（右侧仍同时显示 App 层概览和 StepAgent 层概览） | 保留为 P1，需产品决策后统一右栏职责 |
| P1-5 | 输入栏缺模型选择 | `activeModel` useMemo 依赖 `[]`；InputBar 无法切换模型 | **❌ 仍有效** | `AIAssistantPanel` 只显示当前模型名称/参数，没有模型选择器；用户需在 Settings 里切换模型 | 保留为 P1，可在输入栏右侧增加模型下拉选择 |
| P1-6 | 角色切换无通知 | aiAssistant 自动切换 Agent，界面无提示 | **❌ 仍有效** | `AIAssistantService` 内部会切换 `activeAgentId`，但未插入系统提示消息告知用户 | 保留为 P1，低改动 |
| — | AIAssistantService 绕过 DataService 直接读 localStorage | 旧单体设计，状态不经过 DataService | **🔶 待观察** | `AIAssistantService` 仍独立管理 `state`（messages/tasks/conversations），并通过 `initAutoSave` 自行持久化；但这是其职责范围内的对话状态，是否算"绕过"需看设计意图 | 暂不升级，纳入右侧面板统一方案时一并考虑 |
| — | DependencyResolver 串行模式静默降级 | DAG 解析器在某些情况下串行执行未告警 | **🔶 待确认** | 需深入 `internal/DependencyResolver.ts` 确认是否存在串行降级逻辑；当前测试全部通过 | 低优先级复核 |
| — | `streamingContent` 缺少 RAF 节流 | 流式输出高频 setState 导致卡顿 | **❌ 仍有效** | `ai-assistant/index.ts` 中 `streamingContent` 通过 setState 更新，未见 `requestAnimationFrame` 节流 | 保留为 P2/P1-，性能敏感时再处理 |
| — | App.tsx 内联回调致无效重渲染 | 大量内联函数传递导致子组件重渲染 | **🔶 部分修复** | `stepContent` 已 memo；但 `onUpdateModels` 等仍是内联箭头函数 | 保留为 P2 |
| — | `renderCenter` 返回组件缺 key | 动态组件切换可能触发 React key 警告 | **✅ 已修复** | `useMemo` 返回单一 `stepContent` 节点，各 step 组件由自身内部管理 key | 关闭 |
| — | 工具页字号体系不统一 | 各工具页字体大小不一致 | **❌ 仍有效** | 属于 UI 规范问题，未在本次修复范围内 | 保留为 P2 |

**P1 评估结论**：
- 已覆盖/已修复：4 项（P1-1、P1-2、P1-3、renderCenter key）
- 部分修复：2 项（P1-4、App 内联回调）
- 仍有效：3 项（P1-5、P1-6、streamingContent 节流、工具页字号）
- 待确认：2 项（AIAssistantService 绕过 DataService、DependencyResolver 串行降级）

**有效 P1 精简为 5-7 项**。

---

## 二、P2 可优化项（14 项）

| 原问题 | 原描述 | 当前状态 | 评估依据 | 后续建议 |
|---|---|---|---|---|
| 1366px 布局偏紧 | 小屏幕下内容区被压缩 | **🔶 缓解** | P0-5 两层三栏已修复，App.tsx 不再嵌套 StepAgent 自己的面板；但三栏本身在 1366px 下仍偏紧 | 保留为 P2，考虑可收起侧边栏 |
| CSS 变量/硬编码不一致 | 颜色、字体多处硬编码 | **❌ 仍有效** | 代码中仍存在大量 `style={{ color: 'var(--color-text-muted)' }}` 与 Tailwind 类混用，规范化程度不足 | 保留为 P2 |
| 输入栏缺快捷指令 | 无 `/` 命令或快捷操作 | **❌ 仍有效** | `useChatInput` 未实现快捷指令解析 | 保留为 P2 |
| 无 code splitting | 1.1MB JS 未拆分 | **❌ 仍有效** | `vite build` 仍提示 chunk > 500KB，未配置 `manualChunks` 或动态 import | 保留为 P2 |
| undoRedo 逻辑可疑 | `useUndoRedo` 副作用问题 | **🔶 待确认** | 未深入审查，但测试通过 | 低优先级复核 |
| store timeout 组件卸载后仍执行 | Zustand store 中 setTimeout 未清理 | **🔶 待确认** | 未深入审查所有 stores | 低优先级复核 |
| 流式滚动动画开销 | 消息列表滚动动画性能差 | **❌ 仍有效** | 流式输出时消息区仍在滚动，未见优化 | 保留为 P2 |
| VFile 缺内容类型区分 | 未区分 markdown/text/json 等 | **❌ 仍有效** | `VFile` 类型未增加 `contentType` 字段 | 保留为 P2 |
| nanoid 重复实现 | 多个文件各自实现 nanoid | **🔶 部分存在** | `DataService.ts` 有私有 `nanoid()`，`ai-assistant/index.ts` 也有独立实现；但其他模块多通过导入 `nanoid` | 可统一为 P2 |
| Conversation 压缩丢数据 | 上下文压缩可能丢失信息 | **❌ 仍有效** | `contextCompress.ts` 仍存在压缩逻辑，未评估是否丢数据 | 保留为 P2 |
| agentId 强制类型转换 | `as AgentId` 等强制断言 | **❌ 仍有效** | `ai-assistant/processWithAI.ts` 仍有 `agentMap` 硬编码与类型断言 | 保留为 P2 |
| Electron API 无类型安全 | `window.electronAPI` 为 any | **🔶 部分修复** | `src/renderer/env.d.ts` 已声明 `electronAPI` 类型，但运行时仍可能为 undefined | 保留为 P2 |

**P2 评估结论**：
- 多数 P2 仍为有效优化项
- 没有明显被后续演进覆盖的项
- 建议按"性能 / UI 规范 / 代码质量"分组，分批处理

---

## 三、跨视角共识更新

### 原报告的三条根建议

1. **架构（万鉴识）**："两个系统"风险——`ai-assistant` 与 `agent-runtime` 并存
   - **当前判断**：确认存在，右侧 AI 助手为旧设计，用户已确认"最近才打算更新新 Agent，忘记统一了"
   - **建议**：单独排期，不要混入日常 bug 修复

2. **产品/UI（许清楚）**："布局决定体验"——先修 P0-1 嵌套布局
   - **当前判断**：P0-5 已修复，两层三栏问题已不存在
   - **建议**：降级为 P2，后续关注 1366px 适配

3. **代码质量（寇豆码）**："Hooks 违反不可协商"
   - **当前判断**：P0-9 已修复，App.tsx 早期 return 已处理
   - **建议**：关闭

---

## 四、精简后的待办清单

### 仍有效的 P1（5 项）

| 优先级 | 问题 | 改动量 | 依赖 |
|---|---|---|---|
| 1 | P1-5：输入栏增加模型选择器 | 小 | 无 |
| 2 | P1-6：Agent 角色切换时插入系统提示 | 小 | 无 |
| 3 | P1-4：统一右侧面板职责 | 中 | 需产品决策 |
| 4 | streamingContent RAF 节流 | 中 | 性能测试 |
| 5 | 工具页字号体系统一 | 小 | UI 规范 |

### 仍有效的 P2（8 项，分组）

**性能组**
- 无 code splitting
- 流式滚动动画开销
- 1366px 布局偏紧

**UI 规范组**
- CSS 变量/硬编码不一致
- 输入栏缺快捷指令
- VFile 缺内容类型区分

**代码质量组**
- nanoid 重复实现
- Conversation 压缩丢数据
- agentId 强制类型转换
- undoRedo / store timeout 待复核
- Electron API 类型安全待复核

---

## 五、建议处理顺序

1. **本周**：P1-5 输入栏模型选择器（用户体验提升明显，改动小）
2. **下周**：P1-6 Agent 切换通知 + P1-4 右侧面板统一方案设计
3. **本月**：P2 性能组（code splitting、流式滚动、布局）
4. **持续**：P2 UI 规范组与代码质量组，随功能迭代逐步收敛

---

*本文档基于 `docs/overview.md` 与最新源码交叉评估生成，用于替代原报告中已过时的 P1/P2 状态。*
