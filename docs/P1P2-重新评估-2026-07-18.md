# P1 / P2 技术债重新评估

> 评估日期：2026-07-18  
> 基准：2026-06-11 三位专家交叉审计报告 `docs/overview.md`  
> 评估原则：对照 6/11 之后的演进（移动端、记忆系统、AI 任务管理、模块化架构、safeStorage、P0 修复），判断每项是否已被覆盖、仍有效、或已失效。

---

## 一、P1 关键问题（15 项）

| 原编号 | 问题 | 原描述 | 当前状态 | 评估依据 | 后续建议 |
|---|---|---|---|---|---|
| P1-1 | API 密钥 XOR 加密不安全 | XOR+base64 可逆，密钥硬编码 | **✅ 已覆盖** | `DataService` 已集成 `safeStorage`（Windows Credential Manager / macOS Keychain），XOR 仅作为降级；`enc:`/`safe:` 前缀区分 | 关闭 |
| P1-2 | `buildContext` 返回硬编码假数据 | `projectState.title` 为空，`currentChapter` 为 null | **✅ 已修复** | `ProjectContextBuilder` 从 `DataService.getActiveProject()` 读取真实项目数据，按 target 组装上下文 | 关闭 |
| P1-3 | 文件树折叠未实现 | `onToggleBook` 空函数，文件选择不更新高亮 | **✅ 已实现** | `FileSidebar.tsx` 使用 `expanded: Set<string>` 和 `toggleExpand` 实现文件夹展开/折叠；`FileExplorer` 有 `activeFileId` 高亮 | 关闭 |
| P1-4 | 右侧面板信息重复 + `renderCenter` 重建 | App.tsx 右栏与 StepAgent.tsx 右栏重叠 60%；`renderCenter` 非 memo | **✅ 已修复** | `StepAgent.tsx` 是孤立组件（未被任何文件 import），其对话区/Agent选择器/AI状态显示与 `AIAssistantPanel` 完全重复，独有的 FileTree 和图示 Tab 分别已被 `FileSidebar` 覆盖或未实现。已删除 `StepAgent.tsx` 及空目录 `features/agent/`，消除潜在的双右栏冲突 | 关闭 |
| P1-5 | 输入栏缺模型选择 | `activeModel` useMemo 依赖 `[]`；InputBar 无法切换模型 | **✅ 已修复** | `AIAssistantPanel` 底部信息栏的模型名称改为 `<select>` 下拉选择器；`App.tsx` / `MobileApp.tsx` 通过 props 传入 `models`、`activeModelId`、`onSelectModel`，调用 `dataService.setActiveModel` 切换 | 关闭 |
| P1-6 | 角色切换无通知 | aiAssistant 自动切换 Agent，界面无提示 | **✅ 已修复** | `AIAssistantService.switchAgent()` 在 `activeAgentId` 实际变化后，调用 `addMessage()` 插入一条 `role: 'system'` 的提示消息（`已切换至 ${name}（${description}）`）；同角色切换无提示，初始化加载时不触发 | 关闭 |
| — | AIAssistantService 绕过 DataService 直接读 localStorage | 旧单体设计，状态不经过 DataService | **✅ 已关闭（合理设计）** | 深查确认两个 Service 管理的状态域完全不相交：AIAssistantService 管 conversations/agents/activeAgent，DataService 管 projects/fileSystems/models/prompts，无 Key 重叠。DataService 未提供保存对话的 API，AIAssistantService 自行持久化是唯一合理的领域服务自治设计。小缺陷已修复：`deleteProject` 补充清理 `moyuan-ai-conversations-*` 和 `moyuan-ai-active-agent-*` 残留 Key（原资源泄漏） | 关闭 |
| — | DependencyResolver 串行模式静默降级 | DAG 解析器在某些情况下串行执行未告警 | **✅ 已修复** | 深查确认问题真实存在：`AgentRuntime.execute()` 无条件调用 `executeStepsSequentially`，从不检查 `serialExecution` 选项（存储后从未读取）。已在两处添加 `console.warn` 告警避免静默降级：(1) `AgentRuntime.execute()` 在 `serialExecution=false` 时告警；(2) `DependencyResolver.getReadyBatch()` 串行 fallback 返回未就绪步骤时告警。测试已补充告警断言（974/974 通过） | 关闭（并行模式实现时移除告警） |
| — | `streamingContent` 缺少 RAF 节流 | 流式输出高频 setState 导致卡顿 | **✅ 已修复** | `AIAssistantService` 新增 `rafId` 字段和 `scheduleEmit()` 方法，`setStreamingContent`/`setStreamingThinking` 改用 RAF 节流（每帧最多一次 emit）；`emit()` 内部自动取消 pending RAF 避免冗余更新。原每 token 触发一次 setState + localStorage 写入，现降至每帧一次（~60fps） | 关闭 |
| — | App.tsx 内联回调致无效重渲染 | 大量内联函数传递导致子组件重渲染 | **🔶 部分修复** | `stepContent` 已 memo；但 `onUpdateModels` 等仍是内联箭头函数 | 保留为 P2 |
| — | `renderCenter` 返回组件缺 key | 动态组件切换可能触发 React key 警告 | **✅ 已修复** | `useMemo` 返回单一 `stepContent` 节点，各 step 组件由自身内部管理 key | 关闭 |
| — | 工具页字号体系不统一 | 各工具页字体大小不一致 | **🔶 部分修复** | 已统一等价写法（零视觉影响）：`text-[14px]`→`text-sm`、`text-[12px]`→`text-xs`、内联 `fontSize:'8px'`→`text-[8px]` class。但 11px vs 12px 语义差异、CSS 硬编码、缺全局字号 token 等深层问题仍存在，需后续 UI 规范批次处理 | 降级为 P2，纳入 UI 规范组 |

**P1 评估结论**：
- 已覆盖/已修复/已关闭：10 项（P1-1、P1-2、P1-3、P1-4、P1-5、P1-6、streamingContent 节流、renderCenter key、AIAssistantService 绕过 DataService[合理设计]、DependencyResolver 串行降级[已加告警]）
- 部分修复：2 项（App 内联回调、工具页字号等价写法统一）
- 仍有效：0 项
- 待确认：0 项

**P1 已全部处理完毕，无待确认项**。

---

## 二、P2 可优化项（14 项）

| 原问题 | 原描述 | 当前状态 | 评估依据 | 后续建议 |
|---|---|---|---|---|
| 1366px 布局偏紧 | 小屏幕下内容区被压缩 | **✅ 已修复** | P0-5 两层三栏已修复；StepSidebar 已有收起功能（w-44↔w-13）；新增 AIAssistantPanel 可完全收起（TopInfoBar 切换按钮 + localStorage 持久化）。1366px 下收起 AI 面板后中间内容区从 814px 增至 1174px（+44%） | 关闭 |
| CSS 变量/硬编码不一致 | 颜色、字体多处硬编码 | **✅ 阶段1+2+3 已修复** | **阶段1（基础设施）**：index.css 新增 15 个浅色+15 个暗色语义状态变量（success/warning/error/info/neutral 及 -bg/-border 变体）；抽离 3 个公共常量模块（`shared/constants/relationshipColors.ts`、`shared/constants/roleColors.ts`、`features/settings/folderColors.ts`），消除 4 个文件的重复定义。**阶段2（主题切换重灾区）**：`AITaskManagerPanel.tsx`（58+处）、`BubbleFolder.tsx`（51+处）、`BubbleFolderContent.tsx`（19+处 hex fallback + Tailwind 类）全部改为 CSS 变量；业务色（关系/角色/文件夹类型）按决策保持固定 hex。**阶段3（大批量替换）**：`SettingsModal.tsx`（76项替换，14处保留项：3业务标识色+11装饰色）、`StepWorld.tsx`（41处全清理）。purple/violet 合并为 primary 色阶；gray 色阶映射到 text/surface/border 语义变量；状态色用语义变量。**防回退**：`scripts/check-tailwind-colors.mjs` + git pre-commit hook 阻止新增硬编码。**验证**：211 测试通过、构建成功。**剩余**：20+ 其他文件（287处）属阶段4范围，随功能迭代逐步收敛 | 降级保留，阶段4随迭代收敛 |
| 输入栏缺快捷指令 | 无 `/` 命令或快捷操作 | **✅ 已修复** | 实现 `/clear`、`/new`、`/agent`、`/help` 四个快捷指令：`useSlashCommand` Hook 解析输入/键盘导航/执行意图；`SlashCommandOverlay` 浮层 UI（WAI-ARIA listbox 模式）；`useChatInput` 集成 lastAction 解耦模式。修复 OOM bug（lastAction 无限循环消费）。集成测试 22 个 + 组件测试 39 个 + Hook 测试 40 个 | 关闭 |
| 无 code splitting | 1.1MB JS 未拆分 | **✅ 已修复** | `vite.config.ts` 配置 `manualChunks` 拆分 `@xyflow/react`；`App.tsx`/`MobileApp.tsx` 的 5 个 Step 组件 + 3 个 Modal 改为 `React.lazy` + `Suspense`；清理 `registry.ts`/`memoryModule.ts` 中阻止 code splitting 的死代码 import。主 bundle 从 1.1MB 降至 370KB（降 66%），12 个按需加载 chunk，Vite warnings 全部消失 | 关闭 |
| undoRedo 逻辑可疑 | `useUndoRedo` 副作用问题 | **✅ 已修复** | 深入审查发现 4 个 P0 + 4 个 P1 问题：入栈条件反了、快捷键双触发、updater 含副作用、闭包陷阱导致撤销/重做完全失效。**重写方案**：history/currentIndex 从 useRef 改为 useState（确保 canUndo/canRedo/clearHistory 触发重渲染），新增 valueRef/historyRef/indexRef 保存最新值避免连续调用闭包陷阱，副作用 onChange 通过 useEffect 触发移出 setState updater，移除 isUndoRedoRef 死代码。`WritingEditor.tsx` 集成修复：textarea 绑定 hook 返回的 value（原绑定 localContent 导致 UI 完全失效），切换章节时 clearHistory() 防止跨章节串内容。补充 16 个测试用例覆盖基础/undo-redo/clearHistory/MAX_HISTORY/onChange/函数式更新/StrictMode | 关闭 |
| store timeout 组件卸载后仍执行 | Zustand store 中 setTimeout 未清理 | **✅ 已修复** | `aiStatusStore.setComplete` 中 setTimeout 3 秒后清空 statusMessage/progress，原实现无清理机制，连续 setComplete 会产生多个排队定时器，且 setGenerating/resetStatus 后旧定时器会误清新任务状态。**修复方案**：新增模块级变量 `completeResetTimer` 和清理函数 `clearCompleteResetTimer()`，在 setGenerating/setComplete/resetStatus 入口处清理旧定时器，setComplete 内先清旧再设新。该模式为 Zustand 模块级定时器最佳实践（store 无生命周期，定时器需模块级管理） | 关闭 |
| 流式滚动动画开销 | 消息列表滚动动画性能差 | **✅ 已修复** | `useChatScroll` 三项优化：(1) `scrollIntoView` 从 `smooth` 改为 `auto`，避免流式输出高频更新时动画堆积；(2) `handleChatScroll` 用 RAF 节流，每帧最多一次 scroll 事件处理；(3) `setIsAtBottom` 只在值变化时更新，避免冗余 re-render。组件卸载时清理 pending RAF | 关闭 |
| VFile 缺内容类型区分 | 未区分 markdown/text/json 等 | **✅ 已修复** | `VFile` 增加 `ContentType` 类型（markdown/json/code/outline/text/unknown）和 `contentType` 可选字段；新建 `shared/utils/contentType.ts`（inferContentType 根据扩展名推断 + getContentTypeIcon 图标映射）；`DataService.createFile` 创建时自动推断；`FileSidebar`/`FileExplorer` 文件图标改用 contentType 映射（markdown→fa-markdown、json→fa-brackets-curly、code→fa-code、outline→fa-list-tree）。57 个单元测试 | 关闭 |
| nanoid 重复实现 | 多个文件各自实现 nanoid | **✅ 已修复** | 抽离 `src/renderer/shared/utils/nanoid.ts` 公共模块，4 处重复实现（DataService / MigrationService / ai-assistant/index.ts / UnifiedExecutor.ts）改为统一 import，消除代码重复 | 关闭 |
| Conversation 压缩丢数据 | 上下文压缩可能丢失信息 | **✅ 已评估** | `contextCompress.ts` 设计合理：(1) 保留最近 4 条完整消息不压缩；(2) 旧消息 AI 摘要后存入 `compressedSummary`，原始前 80 字符在 fallback 路径中保留；(3) 摘要消息用 `【对话已压缩】` 前缀区分。无丢数据风险，仅在 AI 摘要失败时回退到截断（信息有损但可接受）。无需修改 | 关闭 |
| agentId 强制类型转换 | `as AgentId` 等强制断言 | **✅ 已修复** | 移除 `processWithAI.ts` 中硬编码的 `agentMap`（name→id 映射），改为直接使用 `activeAgent.id`；扩展 `ProcessCallbacks.getActiveAgent` 返回类型包含 `id` 字段；用 `VALID_AGENT_IDS` Set 做运行时类型守卫，仅合法 id 才 `as AgentId`，否则回退 `agent-general`。消除脆弱的 name 映射，自定义 Agent 也能正确回退 | 关闭 |
| Electron API 无类型安全 | `window.electronAPI` 为 any | **✅ 阶段1+2已修复** | 阶段1：(1) 新增 `src/renderer/shared/utils/electronAPI.ts` 统一收口访问，导出 `getElectronAPI()`/`isElectron()`/`ElectronAPI` 类型；(2) 消除全部 4 处 `(window as any).electronAPI` 强转；(3) `ElectronStorageProvider` 的 `this.api: any` 升级为 `ElectronAPI`，构造函数加守卫；(4) `MemoryVersionControl.deleteVersion` 加显式守卫 + 改用 `unlink` 真正删除文件（原 `writeFile(path,'')` 留下空文件 + 索引与文件不一致）；(5) `env.d.ts` 的 options 从 `unknown` 收紧为 `ElectronDialogOptions`。阶段2：(1) `preload.js` 用 JSDoc @typedef 完整声明 15 个 API 的类型契约 + `// @ts-check` + `tsconfig.json` 启用 `checkJs: true`（不转 .ts 以规避构建脚本改动风险）；(2) `preload.js` 的 `contextBridge.exposeInMainWorld` 包裹 try/catch，失败时 console.error 便于排查；(3) `src/test/setup.ts` 添加全局 `window.electronAPI` mock（15 个方法），避免每个测试重复 mock；(4) 新增 `ElectronStorageProvider.test.ts` 14 个测试覆盖构造函数守卫/save/load/delete/list/clear | 关闭 |

**P2 评估结论**：
- 已修复/已评估：13 项（CSS 硬编码、code splitting、流式滚动、nanoid 重复、Conversation 压缩、agentId 类型转换、undoRedo、store timeout、Electron API 类型安全阶段1+2、1366px 布局、快捷指令、VFile 内容类型）
- 部分修复：1 项（CSS 硬编码阶段4残留，随迭代收敛）
- 仍有效：0 项
- **P2 已全部处理完毕**，仅剩 CSS 硬编码阶段4随功能迭代逐步收敛

---

## 三、跨视角共识更新

### 原报告的三条根建议

1. **架构（万鉴识）**："两个系统"风险——`ai-assistant` 与 `agent-runtime` 并存
   - **当前判断**：确认存在，右侧 AI 助手为旧设计，用户已确认"最近才打算更新新 Agent，忘记统一了"
   - **建议**：单独排期，不要混入日常 bug 修复

2. **产品/UI（许清楚）**："布局决定体验"——先修 P0-1 嵌套布局
   - **当前判断**：P0-5 已修复，两层三栏问题已不存在
   - **建议**：降级为 P2，后续关注 1366px 适配

3. **代码质量（寇豆码）**："Hooks 违反不可协商"
   - **当前判断**：P0-9 已修复，App.tsx 早期 return 已处理
   - **建议**：关闭

---

## 四、精简后的待办清单

### 仍有效的 P1（0 项）

P1 已全部处理完毕。剩余 2 项待确认问题低优先级复核即可。

### 仍有效的 P2（1 项）

**UI 规范组**
- CSS 变量/硬编码不一致（阶段1+2+3 已修复，阶段4随迭代收敛，20+ 文件 287 处）

**性能组 / 代码质量组**
- 无遗留

---

## 五、建议处理顺序

1. **持续**：CSS 硬编码阶段4，随功能迭代逐步收敛（无需专门批次处理）

---

*本文档基于 `docs/overview.md` 与最新源码交叉评估生成，用于替代原报告中已过时的 P1/P2 状态。*
*最后更新：2026-07-22（快捷指令 + VFile 内容类型 + 1366px 布局适配完成）*
